# NN API

The NN API returns fund values for each work day with time being the UTC time of your request. The time is useless, it can be ignored.

For some reason some of the records are duplicated. Take care to remove duplicate dates (again, ignore the time)